# BMR Signal Server — Dependencies

# Market data
yfinance>=0.2.0

# API server
fastapi>=0.100.0
uvicorn>=0.20.0
httpx>=0.24.0      # for TestClient

# Data
numpy>=1.24.0
requests>=2.28.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: FRED macro data (free API key at https://fred.stlouisfed.org)
# Set FRED_API_KEY environment variable before running

# Optional: Pydantic (usually installed with FastAPI)
pydantic>=2.0.0
